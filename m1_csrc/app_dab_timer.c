/* See COPYING.txt for license details. */

/*
*
* app_dab_timer.c
*
* The DAB Timer functionality merged into app_clock.c as the "Timer" page
* of the unified Clock app. This file is intentionally empty so that the
* existing CMakeLists.txt source list keeps building unchanged. The
* `app_dab_timer_run` entry point now lives in app_clock.c and lands the
* user directly on the Timer page.
*
* M1 Project
*
*/
